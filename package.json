{
  "name": "openwriter-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "node scripts/run-checks.mjs",
    "privacy": "node scripts/check-skill-privacy.mjs && node scripts/check-fixture-provenance.mjs",
    "check:lockfile": "node scripts/check-lockfile-sync.mjs"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "packageManager": "npm@11.16.0",
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
